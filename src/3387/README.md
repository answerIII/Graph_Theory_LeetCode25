# 3387. Maximize Amount After Two Days of Conversions

## Описание задачи
Дана начальная валюта `initialCurrency` (1.0 единицы), и два набора пар конвертаций: `pairs1` с курсами `rates1` для первого дня, `pairs2` с курсами `rates2` для второго дня. Каждая пара `[start, target]` позволяет конвертировать `start` в `target` с курсом `rate` и обратно с курсом `1/rate`. Нужно максимизировать количество `initialCurrency` после любых конвертаций за два дня.

**Пример 1**:
```
Input: initialCurrency = "EUR", pairs1 = [["EUR","USD"],["USD","JPY"]], rates1 = [2.0,3.0], pairs2 = [["JPY","USD"],["USD","CHF"],["CHF","EUR"]], rates2 = [4.0,5.0,6.0]
Output: 720.0
```

**Пример 2**:
```
Input: initialCurrency = "NGN", pairs1 = [["NGN","EUR"]], rates1 = [9.0], pairs2 = [["NGN","EUR"]], rates2 = [6.0]
Output: 1.5
```

**Ограничения**:
- `1 <= initialCurrency.length <= 3`
- `1 <= pairs1.length, pairs2.length <= 10`
- `1.0 <= rates1[i], rates2[i] <= 10.0`

## Алгоритм решения
Конвертации представляются как ориентированные графы. Используем модифицированный алгоритм Беллмана-Форда для поиска максимального множителя:
- День 1: находим максимальное количество каждой валюты, начиная с 1.0 `initialCurrency`.
- День 2: для каждой валюты из первого дня максимизируем количество `initialCurrency`.
- Используем логарифмы для преобразования умножения курсов в сложение, чтобы избежать проблем с точностью.

### Математическая основа
- Максимальный множитель эквивалентен поиску пути с максимальным произведением весов в графе.
- Логарифмы позволяют заменить умножение на сложение, что упрощает задачу.
- Поскольку нет циклов с положительным произведением (гарантировано условием), Беллман-Форд работает корректно.

### Сложность
- **Время**: O(V * E) для каждого дня, где V — число валют, E — число пар.
- **Память**: O(V + E) для хранения графа.


### Комментарий
В этот раз с первого раза не получилось. Встретился с ошибкой на 766 тесте из 767 😢

```
Output
10000000000.00032
```
```
Expected
10000000000.00000
```
### Решение проблемы
Чтобы устранить проблему с точностью, мы можем:

1. Округлить результат до приемлемого числа знаков после запятой. 
2. Улучшить алгоритм, чтобы минимизировать использование Math.log и Math.exp, заменив их на прямые умножения.
3. Проверить тест, чтобы понять, где именно возникает большая разница.

### Оптимизация
- Первоначально использовались логарифмы для преобразования умножения в сложение, но это вносило погрешности из-за `Math.log` и `Math.exp`.
- Новый подход использует прямое умножение курсов, что устраняет ошибки округления.
- Решение остаётся эффективным для небольших графов (до 10 пар).

---
# Добиваемся лучшего 😎

## Алгоритм решения
- Используем модифицированный Беллман-Форд, работая напрямую с парами конвертаций:
  - День 1: обновляем максимальные суммы для всех валют, используя прямые и обратные рёбра.
  - День 2: то же самое, но с обратным графом.
- Комбинируем результаты: для каждой промежуточной валюты `k` вычисляем `amountsDay1[k] * amountsDay2[k]`.
- Округляем результат до 5 знаков после запятой.

### Сложность
- **Время**: O(E * E) для Беллмана-Форда на каждый день, где E — число пар (≤ 10). Итог: O(E * E) ≈ O(10 * 10) ≈ O(100) на день, всего O(200).
- **Память**: O(V) для хранения сумм, где V — число валют (≤ 20). Итог: O(20) элементов.

## Последняя реализация
- Используем `Map` для хранения сумм.
- Минимизируем память, работая напрямую с входными парами.