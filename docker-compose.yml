version: "3.8"

services:
  backend:
    build:
      context: .
      dockerfile: backend/Docker.backend
    ports:
      - "8080:8080"
    networks:
      - appnet

  frontend:
    build:
      context: .
      dockerfile: frontend/Docker.frontend
    ports:
      - "3000:80"
    networks:
      - appnet

networks:
  appnet:
    driver: bridge